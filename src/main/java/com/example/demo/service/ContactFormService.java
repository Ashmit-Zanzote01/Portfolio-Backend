package com.example.demo.service;

import com.example.demo.model.ContactForm;

public interface ContactFormService {
    ContactForm saveContactForm(ContactForm contactForm);  // Only method declaration
}
