spring.application.name=demo
server.port=${PORT:8080}

# Database Configuration - Uses Environment Variables on Render
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d39ohqk9c44c73fmjp70-a:5432/portfolio_db_4o1d?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:portfolio_db_4o1d_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:AFL1fw6Om7S9Pt9zujWikGD3xkd3ziLS}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.auto_quote_keyword=true

# Mail Configuration (keep your existing settings)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ashmit.zanzote.dev@gmail.com
spring.mail.password=wash wgkc kstu dopq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Enable JavaMail debug logs
logging.level.org.springframework.mail=DEBUG
logging.level.com.sun.mail=DEBUG

# SMTP Timeouts (prevents hanging)
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Additional production settings for Render
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Hikari Pool Settings for Render Stability
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.max-lifetime=30000
spring.datasource.hikari.keepalive-time=15000
